import styled from "styled-components";

export const Container = styled.ul`
    width: 100%;
    li {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
`